---
id: INIT-2026-0001/F06/G1-LESSONS
type: lessons
model: claude-sonnet-4-6
status: draft
attempts: 0
---
# Gate 1 lessons

**Context.** This is the `G1-LESSONS` unit for feature `INIT-2026-0001/F06`.
Read the feature's `events.jsonl` and the commits on the feature branch.

**Acceptance criteria.** The artifact for this unit exists and is substantive.

**Do not touch.** Source code not owned by this unit, generated directories, secrets, `.git/`. The driver owns all git — edit files only.

**Verification.** The `doc` gates in `.specfuse/verification.yml` (the artifact exists and something changed).

**Escalation triggers.** If the event log is too sparse to complete this unit honestly, say so in the artifact rather than inventing content.
