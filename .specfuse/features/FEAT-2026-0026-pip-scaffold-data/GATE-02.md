---
gate: 2
status: open
---

<!--
Copyright 2026 Specfuse Contributors
Licensed under the Apache License, Version 2.0. See LICENSE.
-->


# Gate 2 — `specfuse init` (skeleton)

Un-drafted. Gate 1's `plan-next` drafts this gate. Intended scope: `specfuse init <repo>`
writes a fresh `.specfuse/` (templates, rules, features/, verification.yml seed,
roadmap + LEARNINGS seeds, VERSION) plus `.gitignore` and `.claude` wiring (CLAUDE.md
`@rules`, settings allowlist, plugin config) from package resources — parity with
init.sh INIT, minus the symlink trick.

## Definition of done

To be written by gate 1's plan-next.
