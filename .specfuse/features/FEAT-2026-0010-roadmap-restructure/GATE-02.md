---
gate: 2
status: awaiting_review
---

# Gate 2 — Driver auto-archive hook (or close-out)

## Definition of done

- If retained: `loop.py` triggers (or prompts) `roadmap-archive` when a
  feature's PLAN flips to `complete`; back-link rendering in the main
  roadmap is polished per Gate 1 retro findings.
- If Gate 1 covered everything: this gate is explicitly closed out by
  `G1-PLAN` with no substantive WUs and a note in `GATE-01-REVIEW.md`
  explaining why.

This gate is currently a skeleton; substantive work units are drafted by
Gate 1's `plan-next` from the retrospective.

## Reflection notes

<Written by the human at review time.>
